include "../lib/io.lib";

{
    int main() {
        var byte msg[2];
        msg[0] = 65;
        msg[1] = 66;
        draw_string(&msg, 2, 0, 0, 1);   // VRAM is already zero on load, so no fill needed
        return peek_byte(VRAM_START + 10);
    }
}
