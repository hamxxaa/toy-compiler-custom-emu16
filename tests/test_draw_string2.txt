include "../lib/io.lib";

{
    int main() {
        var byte msg[2];
        msg[0] = 65;
        msg[1] = 66;
        fill(0);
        draw_string(&msg, 2, 0, 0, 1);
        return peek_byte(VRAM_START + 4);
    }
}
